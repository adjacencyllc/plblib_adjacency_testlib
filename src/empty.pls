start
    winshow             // shows the default PL/B character window
    loop                // loop until an event happens
        waitevent       // Close of the character window will stop the program
    repeat              // repeat loop forever
